server:
  port: 8090

spring:
  application:
    name: alexPro-backend

  datasource:
    url: jdbc:postgresql://localhost:5434/alexpro_db
    username: postgres
    password: "1234"
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

resilience4j.ratelimiter:
  instances:
    gateRateLimiter:
      limitForPeriod: 3
      limitRefreshPeriod: 1m
      timeoutDuration: 0s